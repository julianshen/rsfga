apiVersion: v1
kind: Service
metadata:
  name: {{ include "rsfga.fullname" . }}
  labels:
    {{- include "rsfga.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rsfga.selectorLabels" . | nindent 4 }}
